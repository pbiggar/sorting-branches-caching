branch: 

- very very good, ie none

base:


i:

- ON
- slower step than quicksort, but less fo them
- same order of magnitude as quicksort
= better than quicksort for all cases

c:

- bad performance once its out of the cache
=true
- 2 misses per keya
= 2.5


cache:


better than quicksort


i:
- cache also reduces instruction count
= about 10% better

c:
- abotu half as much
= true

b:
bimodal performs beter


aligned:

c:
- dont know if itll help
